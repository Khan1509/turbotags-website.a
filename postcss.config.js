module.exports = {
  plugins: {
    // Tailwind CSS is a PostCSS plugin
    tailwindcss: {},
    // Autoprefixer adds vendor prefixes to your CSS for broader browser compatibility
    autoprefixer: {},
  },
};
